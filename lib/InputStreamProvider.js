'use strict';

/**
 * The class that provide input stream
 */
class InputStreamProvider {
    /**
     * @constructor
     * @param {*} inputType
     */
    constructor(inputType) {
        this.inputType = inputType;
    }

    /**
     * Get input stream
     * @param {*} inputSource
     */
    getInputStream(inputSource) {
    }
}

module.exports = InputStreamProvider;
